version: '3.8'

services:
  bot:
    build: .
    restart: unless-stopped
    environment:
      BOT_TOKEN: ${BOT_TOKEN}
      N8N_URL_FINANCAS: ${N8N_URL_FINANCAS}
      N8N_URL_COMANDOS: ${N8N_URL_COMANDOS}